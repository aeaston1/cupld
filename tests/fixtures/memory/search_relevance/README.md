This fixture protects deterministic `memory_search` relevance behavior.

It covers title/path matches, alias/tag/heading matches, body matches, multi-term scoring, snippet source selection, tag filtering, no-match behavior, and path-based tie-breaking without starting an MCP server.
